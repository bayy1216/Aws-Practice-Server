# artillery run load-test.yml
# artillery run -o myReport load-test.yml
# artillery report myReport
config:
  target: http://www.reditus.site
  phases:
    - duration: 5
      arrivalRate: 1000

scenarios:
  - flow:
      - get:
          url: "/messages"